# Copy this file to .env and fill in both values.

# Webhook URL — channel Settings → Integrations → Webhooks → New Webhook
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_ID/YOUR_TOKEN

# Bot token — discord.com/developers/applications → your app → Bot → Token
DISCORD_BOT_TOKEN=YOUR_BOT_TOKEN

# Optional: restrict slash commands to one channel (right-click channel → Copy Channel ID)
# Leave blank to allow commands in any channel.
ALLOWED_CHANNEL_ID=
